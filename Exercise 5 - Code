int a=1;
int b= 200;
void setup() {
  size(500,400);
  background(0);
}
void draw() {
  a=(a+1)% width;
  b=(b+12)% height; 
  colorMode(HSB, 220);
  stroke(220);
  fill(a,b,220);
  ellipse(a,b,50, 50);

  
} 
